import React, {Component} from 'react'

class NotFoundPage extends Component{
    render(){
        return(
            <h1>Sorry..you reached page that does not exist</h1>
        )
    }
}

export default NotFoundPage;